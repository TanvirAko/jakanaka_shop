


<div class="container">
    <div class="row my-5 justify-content-center">
        <div class="col-12">
            <a href="/create" class="btn btn-primary m-2 text-center text-capitalize">add new product</a>
            <div class="card ">
              
                <div class="card-body shadow">

<table class="table table-striped">
    <thead>
        <tr>
            <th>#</th>
            <th>Photo</th>
            <th>Name</th>
            <th>Price</th>
            <th>Discount Price</th>
            <th>Stock</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>

 <% products.forEach((item , index) => { %>
    <tr>
        <td><%= index + 1 %></td>
        <td><img src="/product_photo/<%= item.photo %>" alt="" style="height: 60 px; width: 60px; object-fit: cover;"></td>
        <td><%= item.name %></td>
        <td><%= item.daily_price %></td>
        <td><%= item.discount_price %></td>
        <td><%= item.stock %></td>
        
        <td>
            <a href="/single/<%= item.sloge %>" class="btn btn-sm btn-info"><i class="fa fa-eye"></i></a>
            <a href="/edit/<%= item.id %>" class="btn btn-sm btn-warning"><i class="fa fa-edit"></i></a>
            <a href="/single-delete/<%= item.id %>" class="btn btn-sm btn-danger"><i class="fa fa-trash"></i></a>
        </td>
        </tr>
 <% }) %>

       


        
    </tbody>
</table>
                </div>
            </div>
        </div>
    </div>
</div>

